import { React, useState, useEffect } from 'react';
import {
  Button,
  Flex,
  Heading,
  Input,
  Box,
  FormControl,
  FormLabel,
  FormHelperText,
  useToast,
  Wrap,
  WrapItem,
  Spacer,
} from '@chakra-ui/react';

const CustomToast = (props) => {
  return (
    <>
      <Box color="white" p={3} bg="blue.500" mb="100px">
        tests
      </Box>
    </>
  );
};

export default { CustomToast };
